import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App builds', (WidgetTester tester) async {
    // Placeholder — real tests need FRB runtime initialized
    expect(1 + 1, equals(2));
  });
}
